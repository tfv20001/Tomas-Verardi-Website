theme: minima

collections:
  projects:
    output: true
    permalink: /projects/:name/

# Show "Projects" in the top nav (Minima reads header_pages)
header_pages:
  - index.md
  - projects.md
